import { Node } from '@voiceflow/api-sdk';
import { HandlerFactory } from "../Handler";
export declare type NextNode = Node<'next', {
    nextId?: string;
}>;
declare const NextHandler: HandlerFactory<NextNode>;
export default NextHandler;
