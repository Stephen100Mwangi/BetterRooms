import type Fetch from "../fetch";
import { APIKey, SAPIkey } from "../models";
import CrudResource from './crud';
export declare const modelIDKey = "_id";
export declare type ModelIDKey = typeof modelIDKey;
declare class APIKeyResource extends CrudResource<typeof SAPIkey['schema'], ModelIDKey, APIKeyResource> {
    constructor(fetch: Fetch, { resourceEndpoint }?: {
        resourceEndpoint?: string;
    });
    get(id: string): Promise<{
        name: string;
        creatorID: number;
        _id: string;
        workspaceID: string;
        permissions: string[];
        scopes: string[];
        data?: Record<string, unknown> | undefined;
    }>;
    create(workspaceID: string, body: Partial<APIKey>): Promise<APIKey & {
        APIKey: string;
    }>;
    update(id: string, body: Pick<APIKey, 'name' | 'permissions' | 'scopes' | 'data'>): Promise<Partial<APIKey>>;
    delete(id: string): Promise<string>;
}
export default APIKeyResource;
