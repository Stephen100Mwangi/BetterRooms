import { Node } from '@voiceflow/api-sdk';
import { HandlerFactory } from "../Handler";
export declare type ResetNode = Node<any, {
    reset?: boolean;
}>;
/**
 * reset the entire stack to the first flow and it's first node
 */
declare const ResetHandler: HandlerFactory<ResetNode>;
export default ResetHandler;
