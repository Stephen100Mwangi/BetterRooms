import Fetch, { FetchConfig } from "./fetch";
import { APIKey, Diagram, Program, Project, PrototypeProgram, User, Version } from "./resources";
export declare type ClientOptions = {
    options?: FetchConfig;
    clientKey: string;
    apiEndpoint: string;
    authorization?: string;
};
export declare class PublicClient {
    fetch: Fetch;
    project: Project;
    version: Version;
    program: Program;
    diagram: Diagram;
    apiKey: APIKey;
    prototypeProgram: PrototypeProgram;
    constructor({ clientKey, apiEndpoint, authorization, options }: ClientOptions);
}
export declare class Client extends PublicClient {
    user: User;
    constructor({ clientKey, apiEndpoint, authorization, options }: Omit<ClientOptions, 'authorization'> & {
        authorization: string;
    });
}
