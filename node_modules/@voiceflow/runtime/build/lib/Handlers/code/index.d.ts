import { Node } from '@voiceflow/general-types/build/nodes/code';
import { HandlerFactory } from "../../Handler";
export declare type CodeOptions = {
    endpoint?: string | null;
    callbacks?: Record<string, Function>;
    safe?: boolean;
};
declare const CodeHandler: HandlerFactory<Node, CodeOptions | void>;
export default CodeHandler;
