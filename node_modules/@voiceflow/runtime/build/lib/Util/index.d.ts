import { Command } from '@voiceflow/api-sdk';
import Stack from "../Runtime/Stack";
export declare type CommandMatcher<C extends Command = Command> = (command: C, match?: any) => boolean;
export declare const extractFrameCommand: <C extends Command<string, Record<string, unknown>> = Command<string, Record<string, unknown>>>(stack: Stack, matcher: CommandMatcher<C>, match?: any) => {
    index: number;
    command: C;
} | null;
