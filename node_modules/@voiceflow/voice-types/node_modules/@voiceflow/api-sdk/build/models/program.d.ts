import * as s from 'superstruct';
import { BaseCommand, BaseNode, NodeID } from './shared';
export declare const SProgram: s.Struct<{
    id: string;
    variables: string[];
    startId: string;
    skill_id: string;
    lines: Record<string, Record<string, any> & {
        id: string;
        type: string;
    }>;
    commands: (Record<string, any> & {
        type: string;
    })[];
    name?: string | undefined;
}, {
    id: s.Struct<string, any>;
    startId: s.Struct<string, any>;
    skill_id: s.Struct<string, any>;
    name: s.Struct<string | undefined, any>;
    lines: s.Struct<Record<string, Record<string, any> & {
        id: string;
        type: string;
    }>, any>;
    commands: s.Struct<(Record<string, any> & {
        type: string;
    })[], s.Struct<Record<string, any> & {
        type: string;
    }, any>>;
    variables: s.Struct<string[], s.Struct<string, any>>;
}>;
export interface Program<N extends BaseNode = BaseNode, C extends BaseCommand = BaseCommand> extends Omit<s.StructType<typeof SProgram>, 'lines' | 'commands'> {
    lines: Record<NodeID, N>;
    commands: C[];
}
