import { DataAPI } from "../DataAPI";
import { AbstractLifecycle } from "../Lifecycle";
import Runtime, { Options as RuntimeOptions, State as RuntimeState } from "../Runtime";
declare class Controller<R extends any = any, D extends DataAPI = DataAPI> extends AbstractLifecycle {
    private options;
    constructor({ api, handlers, services }: RuntimeOptions<D>);
    createRuntime(versionID: string, state: RuntimeState, request?: R, options?: RuntimeOptions<D>): Runtime<R, D>;
}
export default Controller;
