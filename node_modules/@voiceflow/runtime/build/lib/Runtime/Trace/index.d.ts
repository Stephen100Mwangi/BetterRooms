import { TraceFrame } from '@voiceflow/general-types';
import Runtime from '..';
export default class Trace {
    private runtime;
    private trace;
    constructor(runtime: Runtime);
    addTrace<TF extends TraceFrame>(frame: TF): void;
    get<TF extends TraceFrame>(): TF[];
    debug(message: string): void;
}
