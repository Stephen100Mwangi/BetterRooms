import * as s from 'superstruct';
import { UnknownRecord } from "../types";
export declare const SMember: s.Struct<{
    creatorID: number;
    platformData: Record<string, unknown>;
}, {
    creatorID: s.Struct<number, any>;
    platformData: s.Struct<Record<string, unknown>, any>;
}>;
export declare type Member<P extends UnknownRecord = UnknownRecord> = Omit<s.StructType<typeof SMember>, 'platformData'> & {
    platformData: P;
};
