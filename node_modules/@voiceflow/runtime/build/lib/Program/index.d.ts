import { Command, Node, Program } from '@voiceflow/api-sdk';
declare type MinimalProgram = Partial<Program> & Pick<Program, 'id' | 'lines' | 'startId'>;
export declare class ProgramModel {
    private id;
    private nodes;
    private commands;
    private variables;
    private startNodeID;
    constructor({ id, lines, variables, commands, startId }: MinimalProgram);
    getID(): string;
    getNode(nodeID?: string | null): Node | null;
    getCommands(): Command[];
    getStartNodeID(): string;
    getVariables(): string[];
    getRaw(): Record<string, Node<string, Record<string, unknown>>>;
}
export default ProgramModel;
