import { SlotMappings } from './mappings';
export declare enum EventType {
    INTENT = "intent"
}
export interface BaseEvent {
    type: string;
}
/**
 * @deprecated use BaseEvent or IntentEvent instead
 */
export declare type Event<T extends string = string, D = unknown> = {
    type: T;
} & D;
export interface IntentEvent extends BaseEvent, SlotMappings {
    type: EventType.INTENT;
    intent: string;
}
export interface GeneralEvent extends BaseEvent {
    type: string;
    name: string;
}
export declare type AnyEvent = IntentEvent | GeneralEvent;
export declare const isIntentEvent: (event: BaseEvent) => event is IntentEvent;
export declare const isGeneralEvent: (event: GeneralEvent) => event is GeneralEvent;
